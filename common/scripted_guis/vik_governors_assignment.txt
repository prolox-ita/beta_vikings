scripted_gui = {

	governor_info_window = {
		context_type = selected_state_context
		parent_window_token = selected_state_view
		window_name = "governor_info_window"

		visible = {
			is_owned_by = ROOT
			is_capital = no
		}
		effects = {
			window_info_governors_button_click = {
				set_variable = { open_info = 1 }
			}

		}
	}
	governor_info_panel = {
		context_type = selected_state_context
		parent_window_token = selected_state_view
		window_name = "governor_info_panel"

		visible = {
			check_variable = { open_info = 1 }
		}
		effects = {
			window_info_governors_button_close_click = {
				set_variable = { open_info = 0 }
			}

		}
	}
	
}

